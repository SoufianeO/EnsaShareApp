<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.see_profil"></activity>

        <activity android:name=".view.group_posts" />
        <activity android:name=".view.group" />
        <activity android:name=".view.student_profile" />
        <activity android:name=".view.admin_Profile" />
        <activity android:name=".view.TestActivity" />
        <activity android:name=".view.signupteacher2$testProfile" />
        <activity android:name=".view.signupteacher2" />
        <activity android:name=".view.pop_up_listlevelsteacher" />
        <activity android:name=".view.signup1teacher" />
        <activity android:name=".view.signupstudent2" />
        <activity android:name=".view.signup1" />
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.pop_up_signup" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>